# user-facing requirements, these need to be declared in setup.py install_requires
jsonschema
pyyaml

# developer requirements for running tests
coveralls
flake8
nose

# developer requirements to generate documentation
sphinx

# for building wheels to post on pypi
wheel
